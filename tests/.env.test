# Test Environment Configuration
# This file contains test-specific configurations with small data samples

# Test data directories
RYZE_DATA_ROOT=./tests/data
RYZE_LOGS_DIR=./tests/logs

# Test data paths - using existing sample data
RYZE_NATURE_DATA=${RYZE_DATA_ROOT}/nature_metadata
RYZE_PDF_DIR=${RYZE_DATA_ROOT}/pdfs
RYZE_OCR_OUTPUT=${RYZE_DATA_ROOT}/ocr_results
RYZE_ABSTRACT_DIR=${RYZE_DATA_ROOT}/abstracts
RYZE_FIGURES_DIR=${RYZE_DATA_ROOT}/figures
RYZE_VLM_PREPROCESSING=${RYZE_DATA_ROOT}/vlm_preprocessing
RYZE_SFT_DATA=${RYZE_DATA_ROOT}/sft_data
RYZE_VLM_SFT_DATA=${RYZE_DATA_ROOT}/vlm_sft_data

# Prompt templates directory
RYZE_PROMPTS_DIR=./prompts

# Test API Configuration (use mock or test endpoints)
RYZE_PARSING_API_KEY=test-parsing-api-key
RYZE_QA_API_KEY=test-qa-api-key
OPENAI_API_KEY=test-openai-api-key
OPENAI_BASE_URL=http://localhost:8080/v1  # Mock server

# Test LLM Configuration
RYZE_LLM_MODEL=gpt-3.5-turbo  # Use cheaper model for tests
RYZE_VISION_MODEL=gpt-4-vision
RYZE_QA_RATIO=2  # Generate fewer QA pairs for tests
RYZE_QUALITY_THRESHOLD=2.0

# Test Processing Configuration
RYZE_BATCH_SIZE=2  # Small batch for testing
RYZE_NUM_WORKERS=1  # Single worker for predictable tests
RYZE_GPU_ENABLED=false  # Disable GPU for CI/CD
RYZE_MAX_PAPERS=1  # Process only 1 paper for tests

# Test Server Configuration
RYZE_DOWNLOAD_SERVERS=test-server
RYZE_WORK_HOURS_START=0
RYZE_WORK_HOURS_END=24

# Test Status Server
RYZE_STATUS_PORT=9091  # Different port to avoid conflicts